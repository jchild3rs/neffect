{
  "name": "neffect-monorepo",
  "private": true,
  "version": "0.1.0",
  "engines": {
    "bun": ">=1.2.32"
  },
	"packageManager": "bun@1.2.32",
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "type": "module",
  "scripts": {
    "build": "turbo run build",
    "check": "tsc && biome check --write",
    "dev": "turbo run dev",
    "test": "turbo run test"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.2.2",
    "@types/node": "^24.3.0",
    "turbo": "^2.5.6",
    "typescript": "^5.9.2",
    "@types/bun": "latest"
  }
}
